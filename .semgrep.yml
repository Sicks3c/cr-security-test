rules:
  - id: custom-audit-rule
    patterns:
      - pattern: |
          $FUNC(...)
    message: "Function call detected by the controlled security-test rule."
    severity: INFO
    languages:
      - javascript
      - python
