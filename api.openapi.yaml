openapi: 3.0.3
info:
  title: Controlled API
  version: "1.0"
paths:
  /controlled:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "https://drawn-wendy-limitations-quick.trycloudflare.com/pr28-schema.yaml#/MarkerSchema"
      responses:
        "200":
          description: Controlled response
