# Controlled analyzer isolation check

This inert Markdown fixture invokes the configured custom rule. The rule reports only boolean
isolation results and credential variable names; it never emits a credential value.
